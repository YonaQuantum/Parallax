# @parallax/db

数据库包保存 PARALLAX 的 Prisma schema、migration 和 seed。

常用命令从仓库根目录执行：

```bash
npm run db:generate
npm run db:migrate
npm run db:deploy
npm run db:seed
```

schema 路径：

```txt
packages/db/prisma/schema.prisma
```
