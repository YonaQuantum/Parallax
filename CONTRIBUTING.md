# 贡献指南

感谢你参与 PARALLAX（异视社区）的建设。

这个项目不只接受文章和文档，也欢迎社区成员一起改进社区本体：编辑器、插件、主题、部署脚本、搜索、审核、通知、数据迁移都可以成为贡献方向。

## 开发流程

1. 较大的改动先开 issue 或 discussion 说明想法。
2. Pull Request 尽量聚焦在一个明确问题上。
3. 提交前运行 lint、构建和数据库检查。
4. UI 改动请附截图。
5. 改变部署方式、环境变量或数据结构时，同步更新文档。

## 本地开发命令

```bash
npm install
npm run db:migrate
npm run db:seed
npm run dev
```

## 适合贡献的方向

- 内容编辑器和自定义内容块
- 主题与 UI 组件
- 自托管部署脚本
- 搜索和推荐
- 审核、权限和社区治理
- GitHub、Gitee、Bilibili、RSS 等平台集成
- 文档、示例和迁移指南

## 设计原则

- 自托管优先，不把个人服务器用户变成二等用户。
- 内容要可迁移，避免被某个渲染器或存储服务锁死。
- 插件要可审计，权限要清楚。
- 保持代码边界清晰，让新贡献者能找到入口。
- 先把核心流程做稳，再扩大插件和自动化能力。
